{% extends 'base.html.twig' %}

{% block body %}
	<h1>Langues</h1>


	<table class="nice">
		<tr><th>Titre</th><th>Code</th></tr>
		{% for language in languages %}
		<tr>
			<td><a href="{{ path("languagesShow", {"id": language.getId() }) }}">{{ language.getTitle()|capitalize }}</a></td>
			<td>{{ language.getCode() }}</td>
		</tr>
		{% endfor %}
		<tr><th><a href="{{ path("languagesAdd") }}"><img src="{{ asset("img/add_24.png") }}" title="Ajouter"/></a></th></tr>
	</table>


{% endblock %}

